apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: podinfo
  annotations: {}
spec:
  rules:
    - host: podinfo.${DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: podinfo
                port:
                  name: web
