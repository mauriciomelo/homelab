apiVersion: v1
kind: Service
metadata:
  name: podinfo
spec:
  type: ClusterIP
  selector:
    app: podinfo
  ports:
    - name: web
      port: 9898
      protocol: TCP
      targetPort: web
    - name: http-metrics
      port: 9797
      protocol: TCP
      targetPort: http-metrics
    - name: grpc
      port: 9999
      protocol: TCP
      targetPort: grpc
