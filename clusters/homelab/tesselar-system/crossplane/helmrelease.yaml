---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: crossplane
  namespace: crossplane-system
spec:
  interval: 5m
  chart:
    spec:
      chart: crossplane
      version: "2.0.1"
      sourceRef:
        kind: HelmRepository
        name: crossplane-stable
      interval: 1m
  values:
